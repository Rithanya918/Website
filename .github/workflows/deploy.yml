name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

4. **Scroll down** and click **"Commit changes"**
5. Click **"Commit changes"** again

---

## **✅ What Happens Next:**

1. **GitHub automatically runs** the workflow after you commit
2. **Wait 2-3 minutes**
3. **Go to**: `https://github.com/Rithanya918/Website/actions`
4. **Look for** the new workflow run
5. **It should succeed** with ✅ green checkmark

---

## **🎯 After You Commit:**

**Watch the Actions Tab:**
```
https://github.com/Rithanya918/Website/actions
```

You should see:
- ⏳ **New workflow starting** (yellow dot)
- ⏳ **Building** (takes 2-3 minutes)
- ✅ **Success!** (green checkmark)

---

## **🚀 Then Visit Your Live Site:**

Once you see ✅ green:
```
https://Rithanya918.github.io/Website/
